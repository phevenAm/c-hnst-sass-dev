legacy-peer-deps=false

# Skip the registry audit round-trip and the funding scan on every install.
# Both add several seconds to a cold Vercel install and neither affects output.
audit=false
fund=false
